export interface EmailPasswordCredentials {
    email: string;
    password: string;
}

export interface UserAccount {
    email: string;
    full_name: string;
    profile_picture: string;
    role: string;
    username: string;
}